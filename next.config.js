/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  async redirects(){
    return [
      {
        // source
        // destination
        // permanent
      }
    ]
  }
}

module.exports = nextConfig
